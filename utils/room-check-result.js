class Result {
  /**
   *
   * @param {number} code
   * @param {string} message
   */
  constructor(code, message) {
    this.code = code;
    this.message = message;
  }
}

module.exports = Result;
